## What this changes

One or two sentences. If it fixes an issue, link it.

## Checklist

- [ ] `pnpm build` passes
- [ ] Visual changes: I screenshotted the affected pages in both themes and looked at them
- [ ] Code examples: every printed output in a comment was actually printed when I ran it
- [ ] New or changed figures: I edited the `.excalidraw` source in `drawings/` and
      regenerated the inline SVG with `node scripts/export-figures.mjs` (never by hand)
- [ ] No curly quotes in source (`grep -n "’" <files>` is clean)
- [ ] Nothing pasted from copyrighted sources; everything here is mine to give
- [ ] I license this contribution under the project's licenses (PolyForm NC for code,
      CC BY-NC-SA 4.0 for content)
