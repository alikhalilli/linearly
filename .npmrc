# Project override: the machine-global npmrc enforces minimum-release-age=1440,
# which hides day-old releases; Astro 7's current release train ships several.
# Astro itself is pinned in package.json, so relax the cooldown only here.
minimum-release-age=0
prefer-offline=false
